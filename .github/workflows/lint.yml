name: Lint
on: [push, pull_request]
jobs:
  eslint:
    runs-on: ubuntu-lts
    steps:
      - uses: actions/checkout@v2
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 'lts'
      - name: Install Yarn 4.6.0
        run: npm install -g yarn@4.6.0
      - name: Install dependencies
        run: yarn install
      - name: Run ESLint
        run: yarn eslint . --ext .js,.jsx,.ts,.tsx

